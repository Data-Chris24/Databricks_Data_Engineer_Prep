resources:
  jobs:
    generate_datasets_assoc_s3:
      name: "[DE prep] Generate ASSOC-S3 datasets"
      description: >-
        Writes the clean-dimension teach tables and the slowly-changing-dimension
        assess tables. Asserts the anti-transplant traps are present.
      tasks:
        - task_key: generate
          notebook_task:
            notebook_path: ../../notebooks/datasets/generate_ASSOC-S3.py
      queue:
        enabled: true
